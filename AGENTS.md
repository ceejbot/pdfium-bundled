# pdfium-bundled

## Project memory

This project uses the trivia MCP. All memories are tagged `project:pdfium-bundled`. Recall by that tag at the start of work. Add new lessons via the `session-retro` skill.
